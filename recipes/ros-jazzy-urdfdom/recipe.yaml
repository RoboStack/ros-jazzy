# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-jazzy-urdfdom
  version: 4.0.0
source:
  git: https://github.com/ros2-gbp/urdfdom-release.git
  rev: release/jazzy/urdfdom/4.0.0-3
  target_directory: ros-jazzy-urdfdom/src/work

build:
  script: ${{ 'build_catkin.sh' if unix else 'bld_catkin.bat' }}
  number: 0
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - if: unix
      then:
        - make
        - coreutils
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
    - if: build_platform != target_platform
      then:
        - cross-python_${{ target_platform }}
    - if: build_platform != target_platform
      then:
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - console_bridge
    - numpy
    - pip
    - python
    - ros-jazzy-console-bridge-vendor
    - ros-jazzy-ros-environment
    - ros-jazzy-ros-workspace
    - ros-jazzy-tinyxml2-vendor
    - ros-jazzy-urdfdom-headers
    - ros2-distro-mutex 0.5 jazzy
    - tinyxml2
  run:
    - console_bridge
    - python
    - ros-jazzy-console-bridge-vendor
    - ros-jazzy-ros-workspace
    - ros-jazzy-tinyxml2-vendor
    - ros-jazzy-urdfdom-headers
    - ros2-distro-mutex 0.5 jazzy
    - tinyxml2
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
