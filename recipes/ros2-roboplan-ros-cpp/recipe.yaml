# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-roboplan-ros-cpp
  version: 0.6.1
source:
  git: https://github.com/ros2-gbp/roboplan_ros-release.git
  tag: release/jazzy/roboplan_ros_cpp/0.6.1-1
  target_directory: ros2-roboplan-ros-cpp/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://roboplan-ros.readthedocs.io
  repository: https://github.com/open-planning/roboplan-ros
  license: MIT
  summary: ROS 2 C++ bindings for the RoboPlan motion planning library.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: osx
      then:
        - clang-tools ${{ (cxx_compiler_version ~ '.*') if cxx_compiler_version is defined else '*' }}
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - eigen
    - eigen-abi-devel
    - nanobind
    - numpy
    - pip
    - python
    - ros2-ament-cmake
    - ros2-ament-index-cpp
    - ros2-builtin-interfaces
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-pinocchio
    - ros2-roboplan
    - ros2-roboplan-example-models
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-rosidl-generator-cpp
    - ros2-sensor-msgs
    - ros2-tf2-eigen
    - ros2-trajectory-msgs
    - typing_extensions
  run:
    - eigen
    - eigen-abi-devel
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-pinocchio
    - ros2-roboplan
    - ros2-ros-workspace
    - ros2-rosidl-generator-cpp
    - ros2-sensor-msgs
    - ros2-tf2-eigen
    - ros2-trajectory-msgs
