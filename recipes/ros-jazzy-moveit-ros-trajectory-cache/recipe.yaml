# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-jazzy-moveit-ros-trajectory-cache
  version: 2.12.3
source:
  git: https://github.com/ros2-gbp/moveit2-release.git
  tag: release/jazzy/moveit_ros_trajectory_cache/2.12.3-1
  target_directory: ros-jazzy-moveit-ros-trajectory-cache/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 9
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - pytest
    - python
    - ros-jazzy-ament-cmake
    - ros-jazzy-ament-cmake-pytest
    - ros-jazzy-ament-cmake-uncrustify
    - ros-jazzy-geometry-msgs
    - ros-jazzy-launch-pytest
    - ros-jazzy-launch-testing-ament-cmake
    - ros-jazzy-moveit-common
    - ros-jazzy-moveit-configs-utils
    - ros-jazzy-moveit-planners-ompl
    - ros-jazzy-moveit-resources
    - ros-jazzy-moveit-ros-planning-interface
    - ros-jazzy-rclcpp
    - ros-jazzy-rclcpp-action
    - ros-jazzy-rmf-utils
    - ros-jazzy-robot-state-publisher
    - ros-jazzy-ros-environment
    - ros-jazzy-ros-workspace
    - ros-jazzy-ros2-control
    - ros-jazzy-tf2-ros
    - ros-jazzy-trajectory-msgs
    - ros-jazzy-warehouse-ros-sqlite
    - ros2-distro-mutex 0.10.* jazzy_*
  run:
    - python
    - pyyaml
    - ros-jazzy-geometry-msgs
    - ros-jazzy-moveit-common
    - ros-jazzy-moveit-ros
    - ros-jazzy-moveit-ros-planning-interface
    - ros-jazzy-rclcpp
    - ros-jazzy-rclcpp-action
    - ros-jazzy-ros-workspace
    - ros-jazzy-tf2-ros
    - ros-jazzy-trajectory-msgs
    - ros-jazzy-xacro
    - ros2-distro-mutex 0.10.* jazzy_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
