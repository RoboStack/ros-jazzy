# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-launch-ros
  version: 0.26.12
source:
  git: https://github.com/ros2-gbp/launch_ros-release.git
  tag: release/jazzy/launch_ros/0.26.12-1
  target_directory: ros2-launch-ros/src/work
  patches:
    - patch/ros-jazzy-launch-ros.win.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_python.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_python.bat' }}
  number: 22
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: ROS specific extensions to the launch tool.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - importlib-metadata
    - numpy
    - pip
    - pytest
    - python
    - pyyaml
    - ros2-ament-copyright
    - ros2-ament-flake8
    - ros2-ament-index-python
    - ros2-ament-pep257
    - ros2-composition-interfaces
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-launch
    - ros2-lifecycle-msgs
    - ros2-osrf-pycommon
    - ros2-rclpy
    - ros2-ros-environment
    - ros2-ros-workspace
    - setuptools
  run:
    - importlib-metadata
    - python
    - pyyaml
    - ros2-ament-index-python
    - ros2-composition-interfaces
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-launch
    - ros2-lifecycle-msgs
    - ros2-osrf-pycommon
    - ros2-rclpy
    - ros2-ros-workspace
