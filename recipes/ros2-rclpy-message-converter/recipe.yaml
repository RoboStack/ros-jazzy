# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros2-rclpy-message-converter
  version: 2.0.1
source:
  git: https://github.com/ros2-gbp/rospy_message_converter-release.git
  tag: release/jazzy/rclpy_message_converter/2.0.1-4
  target_directory: ros2-rclpy-message-converter/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_python.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_python.bat' }}
  number: 21
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://ros.org/wiki/rospy_message_converter
  repository: https://github.com/uos/rospy_message_converter
  license: Apache-2.0
  summary: Converts between Python dictionaries and JSON to rclpy messages.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
    - if: target_platform == 'emscripten-wasm32'
      then:
        - ros2-rosidl-default-generators
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - pytest
    - python
    - ros2-ament-copyright
    - ros2-ament-flake8
    - ros2-ament-pep257
    - ros2-builtin-interfaces
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-geometry-msgs
    - ros2-rclpy
    - ros2-rclpy-message-converter-msgs
    - ros2-ros-environment
    - ros2-ros-workspace
    - ros2-rosidl-default-generators
    - ros2-std-msgs
    - ros2-std-srvs
    - ros2-tf2-msgs
    - setuptools
  run:
    - numpy
    - python
    - ros2-distro-mutex 0.16.* jazzy_*
    - ros2-rclpy
    - ros2-ros-workspace
    - ros2-rosidl-parser
    - ros2-rosidl-runtime-py
