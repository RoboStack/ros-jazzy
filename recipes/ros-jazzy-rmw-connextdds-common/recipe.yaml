# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-jazzy-rmw-connextdds-common
  version: 0.22.3
source:
  git: https://github.com/ros2-gbp/rmw_connextdds-release.git
  tag: release/jazzy/rmw_connextdds_common/0.22.3-1
  target_directory: ros-jazzy-rmw-connextdds-common/src/work

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 16
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  license: Apache-2.0
  summary: Common source for RMW implementations built with RTI Connext DDS Professional and RTI Connext DDS Micro.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-jazzy-ament-cmake-ros
    - ros-jazzy-ament-lint-auto
    - ros-jazzy-ament-lint-common
    - ros-jazzy-fastcdr
    - ros-jazzy-rcpputils
    - ros-jazzy-rcutils
    - ros-jazzy-rmw
    - ros-jazzy-rmw-dds-common
    - ros-jazzy-ros-environment
    - ros-jazzy-ros-workspace
    - ros-jazzy-rosidl-runtime-c
    - ros-jazzy-rosidl-runtime-cpp
    - ros-jazzy-rosidl-typesupport-fastrtps-c
    - ros-jazzy-rosidl-typesupport-fastrtps-cpp
    - ros-jazzy-rosidl-typesupport-introspection-c
    - ros-jazzy-rosidl-typesupport-introspection-cpp
    - ros-jazzy-rti-connext-dds-cmake-module
    - ros-jazzy-tracetools
    - ros2-distro-mutex 0.14.* jazzy_*
  run:
    - python
    - ros-jazzy-fastcdr
    - ros-jazzy-rcpputils
    - ros-jazzy-rcutils
    - ros-jazzy-rmw
    - ros-jazzy-rmw-dds-common
    - ros-jazzy-ros-workspace
    - ros-jazzy-rosidl-runtime-c
    - ros-jazzy-rosidl-runtime-cpp
    - ros-jazzy-rosidl-typesupport-fastrtps-c
    - ros-jazzy-rosidl-typesupport-fastrtps-cpp
    - ros-jazzy-rosidl-typesupport-introspection-c
    - ros-jazzy-rosidl-typesupport-introspection-cpp
    - ros-jazzy-rti-connext-dds-cmake-module
    - ros-jazzy-tracetools
    - ros2-distro-mutex 0.14.* jazzy_*
    - if: osx and x86_64
      then:
        - __osx >=${{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
