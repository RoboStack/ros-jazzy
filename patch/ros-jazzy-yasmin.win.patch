diff --git a/include/yasmin/blackboard.hpp b/include/yasmin/blackboard.hpp
index f1410e4..a0ed6ec 100644
--- a/include/yasmin/blackboard.hpp
+++ b/include/yasmin/blackboard.hpp
@@ -16,7 +16,9 @@
 #ifndef YASMIN__BLACKBOARD_HPP_
 #define YASMIN__BLACKBOARD_HPP_

+#if defined(__GNUG__)
 #include <cxxabi.h>
+#endif

 #include <exception>
 #include <map>
diff --git a/src/yasmin/state.cpp b/src/yasmin/state.cpp
index 55aa584..a946b5a 100644
--- a/src/yasmin/state.cpp
+++ b/src/yasmin/state.cpp
@@ -15,7 +15,9 @@

 #include "yasmin/state.hpp"

+#if defined(__GNUG__)
 #include <cxxabi.h> // For abi::__cxa_demangle
+#endif

 #include <algorithm>
 #include <exception>
diff --git a/src/yasmin/state_machine.cpp b/src/yasmin/state_machine.cpp
index 821995d..d36782b 100644
--- a/src/yasmin/state_machine.cpp
+++ b/src/yasmin/state_machine.cpp
@@ -571,8 +571,17 @@ void StateMachine::cancel_state_machine() {
 }

 void StateMachine::set_sigint_handler(bool handle) {
+  sigint_handler_instance = handle ? this : nullptr;
+
+#ifdef _WIN32
+  // MSVC does not provide POSIX sigaction/sigemptyset; use the C signal API.
+  if (handle) {
+    std::signal(SIGINT, sigint_handler);
+  } else {
+    std::signal(SIGINT, SIG_DFL);
+  }
+#else
   if (handle) {
-    sigint_handler_instance = this;
     // Set up signal handler for SIGINT
     struct sigaction sigint_action;
     sigint_action.sa_handler = sigint_handler;
@@ -580,7 +589,6 @@ void StateMachine::set_sigint_handler(bool handle) {
     sigint_action.sa_flags = 0;
     sigaction(SIGINT, &sigint_action, nullptr);
   } else {
-    sigint_handler_instance = nullptr;
     // Reset to default handler
     struct sigaction sigint_action;
     sigint_action.sa_handler = SIG_DFL;
@@ -588,6 +596,7 @@ void StateMachine::set_sigint_handler(bool handle) {
     sigint_action.sa_flags = 0;
     sigaction(SIGINT, &sigint_action, nullptr);
   }
+#endif
 }

 std::string StateMachine::to_string() const {
