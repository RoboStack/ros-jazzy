diff --git a/src/lexical_casts.cpp b/src/lexical_casts.cpp
--- a/src/lexical_casts.cpp
+++ b/src/lexical_casts.cpp
@@ -64,7 +64,9 @@ std::optional<FloatingPointType> parse_floating_point_with_from_chars(const std:

 std::optional<double> stod(const std::string & s)
 {
-#if __cplusplus < 202002L
+// libc++ lacks std::from_chars overloads for floating-point types until LLVM 21,
+// so always use the istringstream path on libc++ (e.g. macOS clang).
+#if __cplusplus < 202002L || defined(_LIBCPP_VERSION)
   // convert from string using no locale
   // Impl with std::istringstream
   std::istringstream stream(s);
@@ -84,7 +86,7 @@ std::optional<double> stod(const std::string & s)

 std::optional<float> stof(const std::string & s)
 {
-#if __cplusplus < 202002L
+#if __cplusplus < 202002L || defined(_LIBCPP_VERSION)
   // convert from string using no locale
   // Impl with std::istringstream
   std::istringstream stream(s);
